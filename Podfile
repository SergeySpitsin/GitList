# Platform Deployment Target
platform :ios, '10.0'

# Ignore all warnings from all pods
inhibit_all_warnings!

target 'GitList' do

  # Use frameworks
  use_frameworks!

  # Network pods
  pod 'Alamofire'

  # Pods for GitList
  pod 'Nuke', '~> 7.0'
  pod 'MarkdownKit'
  
end
